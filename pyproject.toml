[project]
name = "hema2"
version = "0.1.0"
description = "HEMA-2: consolidation-aware tri-memory architecture for lifelong conversational AI (reference implementation)"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
dependencies = []

[tool.setuptools.packages.find]
include = ["hema2*"]
